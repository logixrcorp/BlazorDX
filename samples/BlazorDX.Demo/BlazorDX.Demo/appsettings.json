{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "PowerBI": {
    "//": "When true, /powerbi loads the real powerbi-client SDK and embeds the live Power BI playground sample report. Default false: the demo uses a stub + in-process mock so it works with no tenant and stays deterministic for tests/axe. Production turns it on (appsettings.Production.json).",
    "PlaygroundSample": false
  }
}
